# Pinned exact versions for reproducible installs on free-tier hosts.
# Stage-by-stage additions only; nothing speculative.

fastapi==0.139.2
starlette==1.3.1        # pinned explicitly: pip-audit 2026-07-18 flagged 8 advisories on 0.41.x
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
structlog==24.4.0
python-dotenv==1.2.2    # PYSEC-2026-2270

# storage & retrieval (Stage 2)
faiss-cpu==1.9.0.post1
numpy==1.26.4
psycopg[binary]==3.2.3
redis==5.2.1

# local ONNX embedding + reranking (Stage 3). Raw onnxruntime rather than
# fastembed: the 512MB RAM cap requires session-level memory control
# (arena/prepacking off) that fastembed does not expose -- see
# scripts/measure_memory.py for the numbers.
onnxruntime==1.27.0
tokenizers==0.23.1
huggingface_hub==1.23.0
psutil==7.2.2

# quota windows (Pacific-midnight reset) + observability
tzdata==2026.3
prometheus_client==0.25.0

# -- test/dev only --
pytest==9.1.1           # PYSEC-2026-1845
pytest-cov==7.1.0
httpx==0.27.2
